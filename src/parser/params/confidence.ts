/**
 * M0 源对象直通元素时的置信度。
 *
 * 这里不是"猜得准不准"，而是"这一步用了多少推断"。
 */

/** 图像源对象：既是图像操作符画出来的，类别就不用猜；缺的是资源本身。 */
export const VISUAL_SOURCE_CONFIDENCE = 0.5;

/**
 * 未分类元素（type 为 unknown）。
 *
 * 内容逐字可信，但分类维度一个结论都没有 —— 元素的 confidence 描述的是这个元素
 * 作为一个整体有多可信，因此不能拿内容维度的满分冒充。取中位值，与"几何可信、
 * 语义类别尚未推断"的视觉对象同一档；M1 的分类器落地后按类型分别抬。
 */
export const UNCLASSIFIED_SOURCE_CONFIDENCE = 0.5;
